[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma migrate deploy && npm run db:seed && node src/index.js"

healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
